# Html Examples

Passing in HTML is the easiest, most flexible, and fastest method for generating a document. But, unfortunately, it can be the most confusing and technically challenging.

By using HTML, you have complete control of the content. For example, you can embed images, CSS styles, fonts, and more. In addition, publicly accessible resources, such as images, and fonts from CDNs, can be used and get fetched on rendering.
